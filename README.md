# docker-grizzly-server
A Docker wrapper around Grizzly's archetype Maven project.

## Usage

```bash
docker build -t rxacevedo/docker-grizzly-server
docker run -it -p 8080:8080 rxacevedo/docker-grizzly-server
```
